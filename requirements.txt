# ── core data stuff ───────────────────────────────────────────────
pandas>=2.0
numpy>=1.24           # 1.24+ for pandas-2 compatibility

# ── plotting ──────────────────────────────────────────────────────
plotly>=5.15          # interactive charts (works offline)

# ── notebook front-end (pick one) ────────────────────────────────
jupyterlab            # modern UI  ─ or replace with "notebook" if you prefer
ipywidgets>=8.0       # optional, but Plotly's modebar needs it in classic Notebook
